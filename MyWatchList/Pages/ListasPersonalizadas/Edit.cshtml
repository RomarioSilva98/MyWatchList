@page
@model MyWatchList.Pages.ListasPersonalizadas.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ListaPersonalizada</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ListaPersonalizada.Id" />
            <div class="form-group">
                <label asp-for="ListaPersonalizada.Nome" class="control-label"></label>
                <input asp-for="ListaPersonalizada.Nome" class="form-control" />
                <span asp-validation-for="ListaPersonalizada.Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ListaPersonalizada.Descricao" class="control-label"></label>
                <input asp-for="ListaPersonalizada.Descricao" class="form-control" />
                <span asp-validation-for="ListaPersonalizada.Descricao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ListaPersonalizada.UsuarioId" class="control-label"></label>
                <select asp-for="ListaPersonalizada.UsuarioId" class="form-control" asp-items="ViewBag.UsuarioId"></select>
                <span asp-validation-for="ListaPersonalizada.UsuarioId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
